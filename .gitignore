CLAUDE.md
XSWIFT_Feature_Catalogue.xlsx
CPL_Feature_Catalogue_Plant_Port.xlsx
.env
__pycache__/

gdrive_cookies.txt

dist/
build/

# proposal index rollback backups
embeddings/*.bak

# Vector indexes — vectors now live in Qdrant (FEATURE/PROPOSAL_VECTOR_BACKEND=qdrant).
# Kept locally as a FAISS fallback but no longer committed (large binaries; not used
# on Render, which queries Qdrant). config.py recreates the empty dir at startup.
embeddings/*.faiss
embeddings/*.json

# logs (build, profiling, trace) — all regenerable, never commit
*.log

# local feedback log (ephemeral; durable copy goes to the Google Sheet)
feedback.jsonl
