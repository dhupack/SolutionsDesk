# Web server
Flask>=3.0.0
flask-limiter>=3.5.0
gunicorn>=21.0.0
httpx>=0.27.0

# RAG / LLM
langchain>=0.2.0
langgraph>=0.2.0
langchain-openai>=0.1.0
langchain-groq>=0.1.0
faiss-cpu>=1.7.4
# NOTE: langchain-huggingface + sentence-transformers are intentionally NOT installed.
# They pull in transformers + torch (~190MB RAM), which langchain_core imports at
# startup even on the OpenAI path — that overhead pushed the 512MB free tier into OOM
# during a catalogue rebuild. We use OpenAI embeddings only; the local HuggingFace
# fallback in config.get_embeddings() is disabled. Re-add these two lines if you ever
# switch EMBEDDING_PROVIDER back to a local sentence-transformers model.
#   langchain-huggingface>=0.0.3
#   sentence-transformers>=2.2.0

# Document parsing
pandas>=2.0.0
PyPDF2>=3.0.0
python-pptx>=0.6.21
python-docx>=0.8.11
gdown>=5.1.0
numpy>=1.24.0
python-dotenv>=1.0.0
openpyxl>=3.1.0

# Voice window (desktop .exe) — online_mode.py streams audio to the OpenAI
# Realtime API over a websocket. Pure-Python, tiny; the Render server never
# imports it (it spawns online_mode.py as a subprocess), but the PyInstaller
# build env needs it installed so the .exe can bundle it (see SolutionsDesk.spec).
websocket-client>=1.7.0
